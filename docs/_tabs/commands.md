---
title: Commands
icon: fas fa-terminal
order: 2
---

# Command reference

This is the *human map* for the `lsd-*` command space.

{: .prompt-tip }
If you already know the command name, press `/` and search for it.

## Modules at a glance

- `lsd-python.*` → Conda, environments, bundles (online + air‑gapped)
- `lsd-cuda.*` → NVIDIA/CUDA runtime checks & helpers
- `lsd-docker.*` → container workflows, images, compose helpers
- `lsd-fio.*` → safe user prompts, confirmations, UX helpers
- `lsd-git.*` → repo ergonomics
- `lsd-ops.*` → logging, diagnostics, system inspection

## Full command catalog

The complete command list lives here:

- [Auto-generated command catalog]({{ "/cmds/" | relative_url }})
